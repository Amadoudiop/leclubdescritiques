{#
{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
{{ form_end(form) }}

#}
{% for video in videos %}
    <tr>
        <td>{{ video.iframe|raw }}</td>
        <td>{% if video.publicationdate %}{{ video.publicationdate|date('Y-m-d') }}{% endif %}</td>
    </tr>
{% endfor %}