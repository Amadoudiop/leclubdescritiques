{% extends 'base_back.html.twig' %}

{% block body %}
    <!-- header Page -->
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body app-heading">
                    <img class="profile-img" src="http://placehold.it/150x150">
                    <div class="app-title">
                        <div class="title"><span class="highlight">Pages</span></div>
                        <div class="description">Configure your pages</div>
                    </div>
                    <a href="{{ path('page_new') }}">
                        <button type="button" class="btn btn-success">Add a new Page</button>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-xs-12">
          <div class="card">
            <div class="card-header">
              List of pages
            </div>
            <div class="card-body no-padding">
              <table class="datatable table table-striped primary" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Image</th>
                        <th>Content</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for page in pages %}
                        <tr>
                            <td>{{ page.name }}</td>
                            <td>{{ page.image }}</td>
                            <td>{{ page.content }}</td>
                            <td>
                                <ul>
                                    <li>
                                        <a href="{{ path('page_show', { 'id': page.id }) }}">
                                        <button type="button" class="btn btn-primary" target="blank">show</button></a>
                                    </li>
                                    <li>
                                        <a href="{{ path('page_edit', { 'id': page.id }) }}"><button type="button" class="btn btn-warning">edit</button></a>
                                    </li>
                                </ul>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
              </table>
            </div>
          </div>
        </div>
    </div>

{% endblock %}
